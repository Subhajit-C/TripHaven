<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TripHaven</title>
</head>
<body>
    <h3>Listing Details: </h3>
    
    <ul>
        <li><%= listing.title %></li>
        <li><%= listing.description %></li>
        <li>&#x20B9; <%= new Intl.NumberFormat("en-IN", {maximumFractionDigits: 0}).format(Number(listing.price)) %></li>
        <!-- "Intl.NumberFormat("en-IN", {maximumFractionDigits: 0}).format(Number(listing.price))" this is the syntax to write a number in indian format(eg: 1,99,999) -->
        <li><%= listing.location %></li>
        <li><%= listing.country %></li>
        <!-- <li><img src="<%= listing.image %>" alt="" loading="lazy"></li> -->
        <br>
        <a href="/listings/<%= listing._id %>/edit">Edit this Listing</a>
        <br><br>
        <a href="/listings">Go Back</a>
    </ul>
</body>
</html>